# Harvester VM Network desired-state summary.
# This is a sanitized operator note, not a live export.
# Do not add runtime fields such as uid, resourceVersion, creationTimestamp,
# managedFields, finalizers, or status.
#
# Live verified values:
# - Name: lan-untagged
# - Namespace: talos-cluster
# - Type: UntaggedNetwork
# - Cluster Network: mgmt
# - Bridge: mgmt-br
# - CNI type: bridge
# - CNI version: 0.3.1
# - promiscMode: true
# - IPAM: empty object, so VM IPs are managed by guest/static config or LAN DHCP
# - Purpose: Talos VM LAN access
#
# Sanitized shape:
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: lan-untagged
  namespace: talos-cluster
  labels:
    network.harvesterhci.io/clusternetwork: mgmt
    network.harvesterhci.io/type: UntaggedNetwork
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "name": "lan-untagged",
      "type": "bridge",
      "bridge": "mgmt-br",
      "promiscMode": true,
      "ipam": {}
    }
