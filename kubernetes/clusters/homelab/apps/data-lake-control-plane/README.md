# Data Lake Control Plane App

Placeholder for the future data lake control plane portfolio app.

This app should manage metadata, governance concepts, lineage, and UI/API behavior.

Heavy data warehouse or streaming engines should run as dedicated VMs or separate data services, not inside this app folder by default.
