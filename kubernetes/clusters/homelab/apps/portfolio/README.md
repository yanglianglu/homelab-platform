# Portfolio App

Placeholder for a future portfolio application.

Expected structure:

- `app/`: Deployment, Service, Ingress, ServiceAccount, RBAC
- `config/`: non-secret ConfigMaps
- `storage/`: PVCs if needed
- `secrets/`: secret requirements only, no plaintext secrets
