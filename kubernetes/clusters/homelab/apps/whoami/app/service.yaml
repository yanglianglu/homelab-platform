apiVersion: v1
kind: Service
metadata:
  name: whoami
  namespace: apps
  labels:
    app.kubernetes.io/name: whoami
    app.kubernetes.io/component: smoke-test
    app.kubernetes.io/part-of: homelab-apps
  annotations:
    homelab.yanglianglu.dev/exposure: cluster-internal
    homelab.yanglianglu.dev/next-stage: ingress-tls
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: whoami
  ports:
    - name: http
      port: 80
      targetPort: http
