# Argo CD Applications

Application manifests live here and point to workload manifests elsewhere in `kubernetes/clusters/homelab/`.

Start with explicit Applications. Use ApplicationSet later only when directory conventions are proven and worth automating.
