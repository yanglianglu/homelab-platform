# Argo CD Projects

AppProjects define coarse deployment boundaries.

Initial projects:

- `platform`
- `apps`
- `sandbox`

Keep project permissions simple until there are real multi-team or multi-cluster requirements.
