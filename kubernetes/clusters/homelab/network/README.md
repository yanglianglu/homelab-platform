# Network Domain

This directory owns network and edge-exposure services inside the `homelab-talos` guest cluster.

Examples:

- Cloudflare Tunnel
- external-dns
- public edge routing policy

Do not use this domain for baseline private administration of Harvester, Talos, or Argo CD unless identity and access policy has been designed first.
