apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: harvester-slow-delete
provisioner: driver.harvesterhci.io
parameters:
  host-storage-class: slow
allowVolumeExpansion: true
reclaimPolicy: Delete
volumeBindingMode: Immediate
