# trust-manager

Argo CD-managed trust-manager installation for internal CA bundle distribution.

- Chart: `trust-manager`
- Version: `v0.22.1`
- Repository: `quay.io/jetstack/charts`
- Namespace: `cert-manager`
- Trust source namespace: `cert-manager`

Only ConfigMap targets are enabled. Secret targets remain disabled.
